package valueobjects

// Sku identifies a product — either a configurable (parent) or simple (variant) SKU.
type Sku string

// Money represents a monetary amount in the shopper's local currency.
type Money float64

// Url is a fully-qualified URL string.
type Url string

// StyleSessionId uniquely identifies one combo generation session.
type StyleSessionId string
